import React, {Component} from 'react';

export default class Admin extends Component {
    render() {
        return (
            <div>
                Admin page (todo show admin users here)
            </div>
        );
    }
}
